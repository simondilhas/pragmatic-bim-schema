name: Publish Stable Schema Release to GitHub Pages

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  SCHEMA_ROOT: schema/00_pragmatic_bim_data_contract.yaml

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      release_version:
        description: "Release version (for manual runs), e.g. v1.2.0"
        required: true
        type: string

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages-stable
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      RELEASE_VERSION: ${{ github.ref_type == 'tag' && github.ref_name || inputs.release_version }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          submodules: false

      - name: Restore previously published versions
        run: |
          CACHE_BRANCH="pages-history"
          CACHE_DIR="$(mktemp -d)"
          trap 'git worktree remove "$CACHE_DIR" --force >/dev/null 2>&1 || true; rm -rf "$CACHE_DIR"' EXIT

          mkdir -p site

          if git ls-remote --exit-code --heads origin "$CACHE_BRANCH" >/dev/null 2>&1; then
            git fetch --depth=1 origin "$CACHE_BRANCH"
            git worktree add "$CACHE_DIR" "origin/$CACHE_BRANCH"
            rsync -a --exclude '.git' "$CACHE_DIR"/ site/
          else
            echo "No $CACHE_BRANCH branch found yet; starting fresh."
          fi

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install LinkML and docs tooling
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-docs.txt

      - name: Generate schema outputs
        run: |
          mkdir -p site/schema

          find_cmd() {
            for cmd in "$@"; do
              if command -v "$cmd" >/dev/null 2>&1; then
                echo "$cmd"
                return 0
              fi
            done
            return 1
          }

          CSV_CMD="$(find_cmd gen-csv csvgen)" || { echo "No CSV generator command found"; exit 1; }
          PYDANTIC_CMD="$(find_cmd gen-pydantic pydanticgen)" || { echo "No Pydantic generator command found"; exit 1; }
          DOC_CMD="$(find_cmd gen-markdown gen-docs gen-doc)" || { echo "No docs generator command found"; exit 1; }

          gen-shacl "$SCHEMA_ROOT" > site/schema/pragmatic-bim.shacl.ttl
          gen-json-schema "$SCHEMA_ROOT" > site/schema/pragmatic-bim.schema.json
          "$CSV_CMD" "$SCHEMA_ROOT" > site/schema/pragmatic-bim.csv
          "$PYDANTIC_CMD" "$SCHEMA_ROOT" > site/schema/pragmatic-bim.pydantic.py

          if [ "$DOC_CMD" = "gen-doc" ] || [ "$DOC_CMD" = "gen-docs" ]; then
            "$DOC_CMD" \
              --index-name pragmatic-bim.docs \
              --hierarchical-class-view \
              --truncate-descriptions false \
              --diagram-type mermaid_class_diagram \
              -d site/schema/.md-src \
              "$SCHEMA_ROOT"
          else
            mkdir -p site/schema/.md-src
            "$DOC_CMD" "$SCHEMA_ROOT" > site/schema/.md-src/pragmatic-bim.docs.md
          fi

          python scripts/generate_diagrams.py --skip-readme

          python scripts/build_schema_docs.py \
            --md-src site/schema/.md-src \
            --site-dir site/schema

          case "$RELEASE_VERSION" in
            v[0-9]*)
              ;;
            *)
              echo "RELEASE_VERSION must start with 'v' (got: $RELEASE_VERSION)"
              exit 1
              ;;
          esac

      - name: Generate module resolver pages
        run: |
          python scripts/generate_module_pages.py \
            --site-dir site \
            --schema-dir schema \
            --base-url https://schema.pragmaticbim.ch \
            --write-root-index

      - name: Publish latest and versioned snapshots
        run: |
          mkdir -p "site/latest" "site/$RELEASE_VERSION"

          cp -R site/schema "site/latest/"
          cp -R site/schema "site/$RELEASE_VERSION/"
          cp site/index.html "site/latest/index.html"
          cp site/modules.json "site/latest/modules.json"

          for slug in $(python - <<'PY'
          import json
          from pathlib import Path
          data = json.loads(Path("site/modules.json").read_text(encoding="utf-8"))
          for module in data.get("modules", []):
              slug = module.get("slug")
              if isinstance(slug, str) and slug:
                  print(slug)
          PY
          ); do
            mkdir -p "$(dirname "site/latest/$slug")" "$(dirname "site/$RELEASE_VERSION/$slug")"
            cp -R "site/$slug" "site/latest/$slug"
            cp -R "site/$slug" "site/$RELEASE_VERSION/$slug"
            mkdir -p "$(dirname "site/latest/$slug.html")" "$(dirname "site/$RELEASE_VERSION/$slug.html")"
            cp "site/$slug.html" "site/latest/$slug.html"
            cp "site/$slug.html" "site/$RELEASE_VERSION/$slug.html"
          done

          python scripts/generate_module_pages.py \
            --site-dir "site/$RELEASE_VERSION" \
            --schema-dir schema \
            --base-url "https://schema.pragmaticbim.ch/$RELEASE_VERSION" \
            --release-label "$RELEASE_VERSION" \
            --write-root-index

          python - <<'PY'
          from __future__ import annotations
          import json
          import os
          from pathlib import Path

          release = os.environ["RELEASE_VERSION"]
          versions_file = Path("site/versions.json")
          versions: list[str] = []
          if versions_file.exists():
            try:
              data = json.loads(versions_file.read_text(encoding="utf-8"))
              existing = data.get("versions", [])
              if isinstance(existing, list):
                versions.extend(v for v in existing if isinstance(v, str) and v.startswith("v"))
            except Exception:
              pass

          if release not in versions:
            versions.append(release)

          def semver_key(v: str) -> tuple[int, int, int, str]:
            raw = v[1:] if v.startswith("v") else v
            parts = raw.split(".")
            nums = []
            for p in parts[:3]:
              try:
                nums.append(int(p))
              except ValueError:
                nums.append(0)
            while len(nums) < 3:
              nums.append(0)
            return (nums[0], nums[1], nums[2], v)

          versions = sorted(set(versions), key=semver_key, reverse=True)

          payload = {
            "latest": release,
            "versions": versions,
          }
          versions_file.write_text(json.dumps(payload, indent=2) + "\n", encoding="utf-8")
          PY

      - name: Persist published site cache
        run: |
          CACHE_BRANCH="pages-history"
          CACHE_DIR="$(mktemp -d)"
          trap 'git worktree remove "$CACHE_DIR" --force >/dev/null 2>&1 || true; rm -rf "$CACHE_DIR"' EXIT

          if git ls-remote --exit-code --heads origin "$CACHE_BRANCH" >/dev/null 2>&1; then
            git fetch --depth=1 origin "$CACHE_BRANCH"
            git worktree add "$CACHE_DIR" "origin/$CACHE_BRANCH"
          else
            git worktree add -b "$CACHE_BRANCH" "$CACHE_DIR"
          fi

          rsync -a --delete --exclude '.git' site/ "$CACHE_DIR"/
          touch "$CACHE_DIR/.nojekyll"

          git -C "$CACHE_DIR" config user.name "github-actions[bot]"
          git -C "$CACHE_DIR" config user.email "github-actions[bot]@users.noreply.github.com"
          git -C "$CACHE_DIR" add -A

          if git -C "$CACHE_DIR" diff --cached --quiet; then
            echo "No cache changes to commit."
            exit 0
          fi

          git -C "$CACHE_DIR" commit -m "cache: update published site for $RELEASE_VERSION"
          git -C "$CACHE_DIR" push origin "HEAD:$CACHE_BRANCH"

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site/

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
